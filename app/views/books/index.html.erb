<% @book.each do |book| %>
  <div>
    <%= link_to book_path(book.id) do %>
      <p><!--ユーザ画像--><%= image_tag book.user.get_profile_image(100,100) %></p>
    <% end %>

    <p>Title<br><%= book.title %></p>
    <p>Opinion<br><%= book.body %></p>
    <p><%= book.user.name %></p>
  </div>
<% end %>